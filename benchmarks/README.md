# Benchmarks

Performance and scalability benchmarks (E-FD8).

## Structure

```
benchmarks/
├── throughput/       # Transactions-per-second measurements
├── latency/          # p50/p95/p99 latency distributions
├── memory/           # Memory usage profiles
└── overhead/         # Payload size comparisons (annotated vs plain)
```
